import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {
  // No necesitas la propiedad title si no la estás usando en tu plantilla
}